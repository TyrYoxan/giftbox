<!DOCTYPE html>
<html lang="fr">
    <body>
    <h1>Categories</h1>
    <ul>
        {% for cat in categorie %}
            <li><a href='categories/{{ cat.id }}'>{{ cat.libelle }}</a> | {{ cat.description }}</li>
        {% endfor %}
    </ul>
    </body>
</html>
